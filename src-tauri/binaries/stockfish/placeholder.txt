Rode scripts/fetch-stockfish.ps1 (Windows) ou scripts/fetch-stockfish.sh
(Linux) pra baixar as 3 variantes do Stockfish (avx2/bmi2/sse41-popcnt) aqui.
Este arquivo só existe pra o diretório sobreviver no git antes disso.
